clear all 
close all
clc

%% Generacja prostok¹ta
N = 2048;
t = linspace (20*pi/N, 20*pi, N);
wave = square(t);

plot(wave)
axis([0 2048 -2 2])

%% FFT przed filtracj¹
figure()
bar(abs(fft(wave))) 
axis([0 100])

%% Filtracja
fs = 5*2048
[b,a] = butter(8, 
