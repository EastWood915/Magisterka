function [wynik] = my_thd(signal)

widmo = abs(fft(signal));

G1 = sqrt(widmo(10)^2 + widmo(11)^2 + widmo(12)^2);
Gn = 0;

for n = 2:40
   Gnt = sqrt(widmo(10*n)^2 + widmo(10*n+1)^2 + widmo(10*n+2)^2);
   
end

end